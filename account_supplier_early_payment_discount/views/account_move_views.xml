<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="account_move_view_form" model="ir.ui.view">
        <field name="name">account.move.form.inherit.supplier.early.payment</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_apply_early_payment"
                    type="object"
                    string="Apply Early Payment"
                    invisible="move_type != 'in_invoice' or state != 'posted' or not early_payment_amount or early_payment_credit_note_id"
                    confirm="A credit note for the discount will be posted and applied to this bill, and an activity will follow up the credit note of the supplier. Continue?"
                />
            </xpath>
            <field name="invoice_payment_term_id" position="after">
                <field
                    name="early_payment_date"
                    invisible="move_type != 'in_invoice' or not early_payment_date"
                />
                <field
                    name="early_payment_amount"
                    invisible="move_type != 'in_invoice' or not early_payment_date"
                />
                <field
                    name="early_payment_credit_note_id"
                    invisible="not early_payment_credit_note_id"
                />
            </field>
        </field>
    </record>
</odoo>
